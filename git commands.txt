git init
	- установка git в вызванной папке.
	- создаетс€ скрыта€ папка .git

git config --global user.name "<username>"
git config --global user.email <email>
	- настройка доступа дл€ пользователей
	- добавл€ютс€ в файле .gitconfig

git config --list
	- список настроек

»гнорирование файлов
<file>/
	- игнорирование всей папки

*.<file type>
	- игнорирование файлов определенного типа

git status 
	- проверка статуса файлов.
git status --untracked-files=all
	- отображаютс€ отдельные файлы в игнорируемых каталогах

cd <file>
	- переход в указанную папку

git add .
	- происходит "снимок" файлов и их индексаци€

git rm --cached <file>
	- сн€тие индекса с файла

git commit -a -m"<massage>"
-a 	- автоматическа€ индексаци€ отслеживаемых файлов (?)
-m	- комментарий к коммиту

git checkout -- <file>
	- возвращение файла к состо€нию, которое было проиндексировано последний раз
git checkout -b <branch name>
	- создание и переход на новую ветку с указанным именем
git checkout <branch name>
	- переключение на указанную ветку

git branch
	- список веток
git branch -v
	- более подробна€ информаци€ о ветках
git branch <branch name>
	- создание новой ветки (без перехода на неЄ)

git log
	- логи коммитов

git remote add origin https://github.com/Vechezar/Java-projects.git
	- присваивание имени дл€ пути репозитори€

git remote
	- список именованных путей

git push -u origin master
-u	- ключ, сохран€ющий настройки пути дл€ команды push
origin	- путь репозитори€
master	- выбор ветки, куда проталкивать изменени€



